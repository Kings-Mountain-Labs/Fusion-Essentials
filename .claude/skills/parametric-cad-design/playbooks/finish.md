## Finish

**verify-the-write** (safety invariant) - Confirm the effect through a read other than the tool's claim; read a refusal's reason before retrying when a write reports success; not for a deferred write, confirmed through the poller it names. Prove: `design_get`: the timeline entry the write claims.

**leave-the-artifact-openable** - Leave the saved state valid, name the sketches and components, look at the result, and say what you could not do when the work is handed on; not for a scratch document. Prove: `doc_get`: the document's save state.

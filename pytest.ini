[pytest]
# Unit tests for the MCP tools run OUTSIDE Fusion with a mocked adsk layer.
# See tests/conftest.py for the harness (mock install + isolated tool loader).
testpaths = tests
# Make tests/ importable so test modules can `from conftest import ...`
# without callers having to set PYTHONPATH.
pythonpath = tests
